import 'package:flutter/material.dart';

mixin AppTheme {
  final Color shadowColor = Colors.grey[100];
  final double elevation = 10;
  final cardBorderRadius = BorderRadius.all(Radius.circular(15));
  final Color textColor = Colors.black;
}
